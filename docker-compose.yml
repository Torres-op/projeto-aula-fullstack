services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - dados:/var/lib/postgresql/data
    # Avisa quando o banco está pronto para receber conexões
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -U ${DB_USER} -d ${DB_NAME}"]
      interval: 2s
      retries: 15

  web:
    build: .
    env_file: .env
    environment:
      DB_HOST: db
    ports:
      - "8000:8000"
    # Alterações no código aparecem sem precisar recriar a imagem
    volumes:
      - .:/app
    depends_on:
      db:
        condition: service_healthy

volumes:
  dados:
